import { Twitter, Linkedin, Facebook, Instagram } from "lucide-react";

// Thresholds for progress-bar / feedback colour, expressed as % of the limit used
export const WARN_AT = 0.75; // amber from here
export const DANGER_AT = 0.95; // red from here
